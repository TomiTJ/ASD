# Starter pipeline

trigger:
- master

pool:
  vmImage: ubuntu-latest

variables:
  # 🔧 Fill these in (or set as pipeline variables)
  AZ_SUB_CONN: 'Azure subscription 1(22c01a58-733d-405e-8882-0da91e1f052d)'  # Service connection name
  APP_SERVICE_NAME: 'bankapp-dev'    # Your Web App name
  RESOURCE_GROUP: 'rg-bankapp'       # Resource group of that Web App
  ARTIFACT_NAME: 'drop'
  RUNTIME: 'JAVA|17'

steps:
- task: JavaToolInstaller@1
  inputs:
    versionSpec: '17'
    jdkArchitectureOption: 'x64'
    jdkSourceOption: 'PreInstalled'

- script: java -version
  displayName: 'Check Java version'

# ✅ Your existing tests against Postgres
- task: Maven@4
  displayName: 'Maven clean test (Postgres test DB)'
  inputs:
    mavenPomFile: 'pom.xml'
    goals: 'clean test -B'
    publishJUnitResults: true
    testResultsFiles: '**/surefire-reports/TEST-*.xml'
    javaHomeOption: 'JDKVersion'
    mavenVersionOption: 'Default'
    mavenAuthenticateFeed: false
    effectivePomSkip: false
    sonarQubeRunAnalysis: false

# 📦 Build JAR (skip tests because we already ran them)
- task: Maven@4
  displayName: 'Maven package (skip tests)'
  inputs:
    mavenPomFile: 'pom.xml'
    goals: 'package -DskipTests -B'
    javaHomeOption: 'JDKVersion'
    mavenVersionOption: 'Default'

# ⬆️ Publish the JAR as a pipeline artifact
- task: PublishBuildArtifacts@1
  displayName: 'Publish JAR artifact'
  inputs:
    PathtoPublish: '$(System.DefaultWorkingDirectory)/**/target/*.jar'
    ArtifactName: '$(ARTIFACT_NAME)'
    publishLocation: 'Container'

# ⚙️ (Optional but recommended) Ensure runtime + app settings on the Web App
# Provide SPRING_DATASOURCE_URL/USERNAME/PASSWORD as pipeline variables (mark secrets!)
- task: AzureCLI@2
  displayName: 'Configure App Service runtime & settings'
  inputs:
    azureSubscription: '$(AZ_SUB_CONN)'
    scriptType: bash
    scriptLocation: inlineScript
    inlineScript: |
      az webapp config set -g "$(RESOURCE_GROUP)" -n "$(APP_SERVICE_NAME)" --linux-fx-version "$(RUNTIME)"
      az webapp config appsettings set -g "$(RESOURCE_GROUP)" -n "$(APP_SERVICE_NAME)" --settings \
        SPRING_PROFILES_ACTIVE=dev \
        WEBSITES_PORT=8080 \
        SPRING_DATASOURCE_URL="$(SPRING_DATASOURCE_URL)" \
        SPRING_DATASOURCE_USERNAME="$(SPRING_DATASOURCE_USERNAME)" \
        SPRING_DATASOURCE_PASSWORD="$(SPRING_DATASOURCE_PASSWORD)"

# 🚀 Deploy JAR to Azure App Service (Linux)
- task: AzureWebApp@1
  displayName: 'Deploy to Azure App Service'
  inputs:
    azureSubscription: '$(AZ_SUB_CONN)'
    appType: webAppLinux
    appName: '$(APP_SERVICE_NAME)'
    package: '$(System.DefaultWorkingDirectory)/**/target/*.jar'

# 🩺 Basic health check (requires spring-boot-starter-actuator)
- script: |
    URL="https://$(APP_SERVICE_NAME).azurewebsites.net/actuator/health"
    echo "Checking $URL"
    for i in {1..20}; do
      code=$(curl -s -o /dev/null -w "%{http_code}" "$URL")
      if [ "$code" = "200" ]; then
        echo "Healthy ✅"
        exit 0
      fi
      echo "Waiting...($i) got $code"; sleep 10
    done
    echo "Health check failed ❌"; exit 1
  displayName: 'Health check'

# Your existing Postgres test DB service (unchanged)
services:
  postgres:
    image: postgres:14
    env:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bankdb_test
    ports:
      - 5432:5432